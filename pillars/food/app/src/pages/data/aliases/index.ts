/**
 * Barrel for the `/food/data/aliases` tab content.
 *
 * The top-level `AliasesTab` wrapper imports from here so the file-tree
 * exposes only one entry point per tab.
 */
export { AliasesTabContent } from './AliasesTabContent';
