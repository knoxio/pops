/**
 * Vestigial router-type alias. The food wire surface is the REST contract
 * in `rest.ts`; the committed `openapi/food.openapi.json` snapshot is the
 * typed source for external consumers (e.g. iOS Swift codegen).
 */
export type FoodRouter = unknown;
