/**
 * Structural snapshot of the lists pillar's public surface. The contract type
 * lives in `manifest.generated.ts`, regenerated by the `generate:manifest`
 * script. This file is the stable `@pops/lists/manifest` import subpath, so
 * consumers don't track the generator output.
 *
 * It also exports the runtime `listsManifest` value consumed when the pillar
 * self-registers with the registry. Lists declares no settings dimension.
 */
import type { ModuleManifest } from '@pops/types';

export type { ListsContract } from './manifest.generated.js';

export const listsManifest: ModuleManifest = {
  id: 'lists',
  name: 'Lists',
  version: '0.1.0',
  surfaces: ['app'],
  description: 'Generic lists — shopping, packing, todo. Food is the first consumer.',
};
