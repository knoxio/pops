{
  "name": "@pops/module-registry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./settings": {
      "types": "./dist/settings/index.d.ts",
      "default": "./dist/settings/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch --preserveWatchOutput",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "registry:build": "tsx scripts/build.ts"
  },
  "dependencies": {
    "@pops/types": "workspace:*"
  },
  "devDependencies": {
    "@pops/cerebrum-contract": "workspace:*",
    "@pops/core-contract": "workspace:*",
    "@pops/finance-contract": "workspace:*",
    "@pops/food-contract": "workspace:*",
    "@pops/inventory-contract": "workspace:*",
    "@pops/lists-contract": "workspace:*",
    "@pops/media-contract": "workspace:*",
    "@types/node": "^25.9.3",
    "@vitest/coverage-v8": "^4.1.8",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
