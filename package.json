{
  "name": "@pops/monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "test:coverage": "turbo test:coverage",
    "lint": "oxlint --type-aware",
    "lint:fix": "oxlint --type-aware --fix",
    "lint:boundaries": "depcruise --config .dependency-cruiser.cjs --ignore-known --output-type err apps/pops-api/src/modules packages/app-ai/src packages/app-cerebrum/src packages/app-finance/src packages/app-inventory/src packages/app-media/src",
    "lint:boundaries:baseline": "depcruise --config .dependency-cruiser.cjs --output-type baseline --output-to .dependency-cruiser-known-violations.json apps/pops-api/src/modules packages/app-ai/src packages/app-cerebrum/src packages/app-finance/src packages/app-inventory/src packages/app-media/src",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "ci:fix": "pnpm lint:fix && pnpm format && pnpm typecheck",
    "registry:build": "pnpm --filter @pops/module-registry registry:build",
    "prepare": "husky"
  },
  "devDependencies": {
    "dependency-cruiser": "^17.4.2",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.4",
    "oxfmt": "^0.52.0",
    "oxlint": "^1.67.0",
    "oxlint-tsgolint": "^0.23.0",
    "turbo": "^2.9.15"
  },
  "packageManager": "pnpm@10.32.1",
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild",
      "msw",
      "sharp"
    ],
    "overrides": {
      "cssstyle": "6.0.2"
    }
  }
}
