name: FE Quality

on:
  push:
    branches: [main]
    paths:
      - "apps/pops-shell/**"
      - "packages/app-*/**"
      - "packages/db-types/**"
      - "packages/ui/**"
      - "packages/api-client/**"
      - "packages/widgets/**"
      - "packages/navigation/**"
      - ".github/workflows/fe-quality.yml"
  pull_request:

jobs:
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    outputs:
      relevant: ${{ steps.filter.outputs.shell }}
    steps:
      - uses: actions/checkout@v6
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            shell:
              - 'apps/pops-shell/**'
              - 'packages/app-*/**'
              - 'packages/db-types/**'
              - 'packages/ui/**'
              - 'packages/api-client/**'
              - 'packages/widgets/**'
              - 'packages/navigation/**'
              - '.github/workflows/fe-quality.yml'

  quality:
    name: Quality Checks
    runs-on: ubuntu-latest
    needs: [changes]
    if: always()
    steps:
      - name: Skip — no relevant changes
        if: github.event_name == 'pull_request' && needs.changes.outputs.relevant != 'true'
        run: echo "No shell/UI changes detected — skipping quality checks"

      - uses: actions/checkout@v6
        if: github.event_name == 'push' || needs.changes.outputs.relevant == 'true'

      - name: Install pnpm
        if: github.event_name == 'push' || needs.changes.outputs.relevant == 'true'
        uses: pnpm/action-setup@v5

      - uses: actions/setup-node@v6
        if: github.event_name == 'push' || needs.changes.outputs.relevant == 'true'
        with:
          node-version: "22"
          cache: pnpm

      - name: Install dependencies
        if: github.event_name == 'push' || needs.changes.outputs.relevant == 'true'
        run: pnpm install --frozen-lockfile

      - name: Build shared packages
        if: github.event_name == 'push' || needs.changes.outputs.relevant == 'true'
        run: cd packages/db-types && pnpm build

      - name: Type check
        if: github.event_name == 'push' || needs.changes.outputs.relevant == 'true'
        run: cd apps/pops-shell && pnpm typecheck

      - name: Lint
        if: github.event_name == 'push' || needs.changes.outputs.relevant == 'true'
        run: cd apps/pops-shell && pnpm lint

      - name: Format check
        if: github.event_name == 'push' || needs.changes.outputs.relevant == 'true'
        run: cd apps/pops-shell && pnpm format:check

      - name: Test
        if: github.event_name == 'push' || needs.changes.outputs.relevant == 'true'
        run: cd apps/pops-shell && pnpm test

      - name: Build
        if: github.event_name == 'push' || needs.changes.outputs.relevant == 'true'
        run: cd apps/pops-shell && pnpm build
