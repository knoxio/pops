name: Inventory Pillar Quality

# Comprehensive standalone quality gate for the collapsed inventory pillar.
# Runs typecheck, test, lint, format, build, OpenAPI generation, and
# Docker build for the inventory pillar in isolation. Triggered ONLY by
# changes that touch the pillar or its transitive @pops/* deps + the
# repo-wide tsconfig/lockfile surface.
#
# Bubble-of-happiness rule: this workflow is the one that MUST stay
# green for the inventory pillar. Other workflows may be red while the
# rest of the codebase still imports the dead @pops/inventory-* package
# names — that's deliberate and will be resolved as each consumer
# migrates to `@pops/inventory`.

on:
  push:
    branches: [main]
    paths:
      - "pillars/inventory/**"
      - "packages/pillar-sdk/**"
      - "packages/types/**"
      - "packages/cerebrum-contract/**"
      - "packages/core-contract/**"
      - "packages/core-db/**"
      - "packages/finance-contract/**"
      - "packages/media-contract/**"
      - "packages/media-db/**"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "tsconfig.base.json"
      - ".oxfmtrc.json"
      - ".github/workflows/inventory-quality.yml"
  pull_request:
    paths:
      - "pillars/inventory/**"
      - "packages/pillar-sdk/**"
      - "packages/types/**"
      - "packages/cerebrum-contract/**"
      - "packages/core-contract/**"
      - "packages/core-db/**"
      - "packages/finance-contract/**"
      - "packages/media-contract/**"
      - "packages/media-db/**"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "tsconfig.base.json"
      - ".oxfmtrc.json"
      - ".github/workflows/inventory-quality.yml"

concurrency:
  group: inventory-quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Inventory pillar — typecheck + test + lint + format + build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v5

      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: pnpm

      - name: Install (subgraph only)
        run: pnpm install --frozen-lockfile --filter "@pops/inventory..."

      - name: Format check
        run: pnpm exec oxfmt --check pillars/inventory/

      - name: Lint
        run: pnpm exec oxlint pillars/inventory/src

      - name: Build transitive deps
        run: pnpm --filter "@pops/inventory^..." build

      - name: Typecheck
        run: pnpm --filter @pops/inventory typecheck

      - name: Build pillar
        run: pnpm --filter @pops/inventory build

      - name: Verify OpenAPI snapshot is up to date
        run: |
          pnpm --filter @pops/inventory generate:openapi
          git diff --exit-code pillars/inventory/openapi/inventory.openapi.json

      - name: Verify manifest.generated.ts is up to date
        run: |
          pnpm --filter @pops/inventory generate:manifest
          git diff --exit-code pillars/inventory/src/contract/manifest.generated.ts

      - name: Test
        run: pnpm --filter @pops/inventory test

  docker:
    name: Inventory pillar — Docker image builds
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: docker/setup-buildx-action@v3

      - name: Build pillar image (builder stage only — fast feedback)
        if: github.event_name == 'pull_request'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: pillars/inventory/Dockerfile
          target: builder
          push: false

      - name: Build pillar image (full)
        if: github.event_name == 'push'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: pillars/inventory/Dockerfile
          push: false
          tags: pops-inventory:${{ github.sha }}
